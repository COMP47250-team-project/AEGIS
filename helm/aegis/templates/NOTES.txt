AEGIS ({{ .Chart.AppVersion }}) deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Ingress hosts:
  App : http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.appHost }}
  API : http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.apiHost }}  (health: /healthz)

{{- if .Values.localServices.enabled }}

Local dev (minikube): in-cluster Postgres + Azurite are running.
  make url        # print the live URLs
  make expose     # reach the app from another machine on the LAN
  make k8s-seed   # pre-create blob containers + load demo data
{{- else }}

AKS: using managed Azure Postgres/Blob. Ensure the backend-secret and the
acr-pull imagePullSecret exist in the namespace (the deploy workflow creates
both). HPA is {{ if .Values.hpa.enabled }}enabled{{ else }}disabled{{ end }}.
{{- end }}

Check status:
  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  kubectl get pods,svc,ingress -n {{ .Release.Namespace }}

The migration Job runs as a post-install/post-upgrade hook; watch it with:
  kubectl logs -n {{ .Release.Namespace }} job/migrate
