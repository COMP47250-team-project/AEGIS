apiVersion: v2
name: aegis
description: AEGIS — Adaptive Exam Guardian and Integrity System (backend + frontend + migration, with optional in-cluster datastores for local dev)
type: application
# Chart version — bump on chart changes. appVersion tracks the app release.
version: 0.1.0
appVersion: "1.0.0"
